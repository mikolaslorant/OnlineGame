public enum MessageType
{
    ACK = 0,
    Input = 1,
    Snapshot = 2,
    ConnectionRequest = 3,
    ConnectionResponse = 4,
    DisconnectionRequest = 5,
    DisconnectionResponse = 6,
}
